{
  "name": "@reponova/trust-configure",
  "version": "0.0.0",
  "private": true,
  "description": "Bulk-configure npm OIDC Trusted Publisher for every @reponova/lang-* package in the monorepo.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo 'trust-configure is invoked via tsx - no build needed'"
  },
  "devDependencies": {
    "node-pty": "^1.1.0",
    "tsx": "^4.19.0"
  },
  "license": "MIT"
}
